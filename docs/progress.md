# 项目进度日志

## 当前状态
- 项目阶段：开发中
- 最后更新：2026-02-27
- 最后完成的工作：清理选题页历史乱码文案（tsc/build 已通过）

## 待办事项
- [ ] 列的拖拽排序功能
- [ ] 导出格式多样化（纯文本/HTML/带格式剪贴板）

## 已完成的工作（最近的在最前面）
### 2026-02-27 - 选题页历史乱码文案全量清理（可见区域）
- 做了什么：
  1. 清理 `TopicList.tsx` 中选题卡片、筛选栏、看板搜索、空状态、新建/编辑弹窗的乱码文案。
  2. 修复看板视图切换按钮的乱码标题与符号，改为可读中文。
  3. 同步清理文件内多处乱码注释，避免后续维护混淆。
- 修改了哪些文件：
  - src/renderer/pages/TopicList.tsx
  - docs/progress.md
- 需要注意的问题：
  - `npx tsc --noEmit` 已通过。
  - `npm run build` 已通过（在沙箱外执行验证）。

### 2026-02-27 - 类型系统与事件监听修复
- 做了什么：
  1. 重写 `App.tsx` 的 `Topic/Config` 类型定义，修复被乱码破坏的字段声明。
  2. 增加选题与配置加载归一化，兼容历史数据缺字段情况。
  3. 修复 preload 菜单事件监听：`onMenuNewTopic/onMenuExport` 改为返回取消监听函数，并在 `App` 中 cleanup。
  4. 同步 main 进程默认配置与 `get-config` 回包结构，确保 `creationConstitutions` 始终可用。
  5. 修复 `TopicList.tsx` 中状态变更事件签名、选题新建字段缺失等 TypeScript 报错。
- 修改了哪些文件：
  - src/renderer/App.tsx
  - src/preload/index.ts
  - src/main/index.ts
  - src/renderer/pages/TopicList.tsx
  - docs/progress.md
- 需要注意的问题：
  - 目前 `tsc --noEmit` 与 `npm run build` 均通过。
  - 仍有一些历史乱码文案未全量清理（不影响编译）。

### 2026-02-27 - 选题页 AI 生成选题乱码修复
- 做了什么：
  1. 修复“AI 生成选题”输入区标题、按钮、提示语乱码。
  2. 修复模板菜单文案乱码，恢复正常中文模板内容。
  3. 修复生成与重新生成的提示词内容乱码，错误提示改为可读中文。
  4. 修复生成结果弹窗中的标题、说明、按钮文案乱码。
- 修改了哪些文件：
  - src/renderer/pages/TopicList.tsx
  - docs/progress.md
- 需要注意的问题：
  - 选题页其余区域仍有历史乱码（非本次范围），建议后续单独做一次全页文案清理。

### 2026-02-27 - 创作页单区即时预览（方案2）
- 做了什么：
  1. 删除“输入区 + 预览区”双区结构，改为单一编辑面。
  2. 聚焦时进入编辑态（`contenteditable`，显示 Markdown 原文）；失焦后进入预览态（渲染 Markdown）。
  3. 保留并适配“选中文字润色”能力：选区索引从 `textarea` 改为 `contenteditable` 选区计算。
  4. 粘贴默认转纯文本，避免带入外部 HTML 样式。
- 修改了哪些文件：
  - src/renderer/pages/Editor.tsx
  - docs/progress.md
- 需要注意的问题：
  - 当前编辑态仍按纯 Markdown 文本输入，不是所见即所得富文本编辑。
  - 在编辑态下，点击其他区域会自动切回渲染态。

### 2026-02-27 - 选题页 AI 生成选题体验优化与乱码修复
- 做了什么：
  1. AI 生成输入改为多行输入框，支持粘贴完整需求，补充快捷键提示（Ctrl/Command + Enter）。
  2. 模板下拉改为可完整展示（不再省略号截断），并优化弹层尺寸与滚动。
  3. 生成结果弹窗支持更大视窗和滚动，选题标题/描述支持完整显示（含 title 提示）。
  4. 修复 `TopicList.tsx` 内多处乱码导致的 JSX 闭合/字符串异常，恢复可编译状态。
- 修改了哪些文件：
  - src/renderer/pages/TopicList.tsx
  - docs/progress.md
- 需要注意的问题：
  - `TopicList.tsx` 仍存在不少历史乱码文案（功能已可用）；建议下次单独做一次“全量文案清理”。

### 2026-02-27 - 工具栏简化为右上角更多菜单
- 做了什么：
  1. 删除编辑器原工具栏样式，改成更轻的页头结构。
  2. 将“导出”从显性按钮移到右上角“···”更多菜单。
  3. 保留“返回”和“创作提示词”入口，避免打断原流程。
- 修改了哪些文件：
  - src/renderer/pages/Editor.tsx
- 需要注意的问题：
  - 导出入口现在在右上角“···”菜单中。

### 2026-02-27 - 删除全文润色按钮（与 AI 助手合并）
- 做了什么：
  1. 删除创作页工具栏“全文润色”按钮。
  2. 删除对应的全文润色函数与加载状态，避免重复入口和死代码。
- 修改了哪些文件：
  - src/renderer/pages/Editor.tsx
- 需要注意的问题：
  - 现在全文改文请走 AI 助手入口（先生成建议，再预览应用）。

### 2026-02-27 - 创作页即时预览（本次）
- 做了什么：
  1. 创作页从“左右分栏预览”改成“单栏即时预览”，预览放到编辑区下方并实时更新。
  2. 删除工具栏“显示/隐藏预览”按钮，减少分心操作。
  3. 保留“导出”“提示词”等核心入口，流程不变。
- 修改了哪些文件：
  - src/renderer/pages/Editor.tsx
- 需要注意的问题：
  - 现在默认始终展示即时预览，不再支持手动开关预览面板。

### 2026-02-27 - 创作页去掉「撤销AI / 重做AI」
- 做了什么：
  1. 删除创作页工具栏的「撤销AI」「重做AI」按钮。
  2. 删除对应 AI 历史栈快捷键逻辑（Ctrl/Cmd+Z、Ctrl/Cmd+Y）。
- 修改了哪些文件：
  - src/renderer/pages/Editor.tsx
- 需要注意的问题：
  - 回退建议在“差异预览”阶段确认后再应用。

### 2026-02-27 - 创作页起步分流（直接初稿 / 先聊再写）
- 做了什么：
  1. 新增创作起步分流：直接生成初稿、先聊一聊再写。
  2. direct 路径支持“先预览再应用”，chat 路径默认展开 AI 助手。
- 修改了哪些文件：
  - src/renderer/pages/Editor.tsx
  - src/renderer/pages/FloatingBall.tsx
- 需要注意的问题：
  - 仅在“正文为空且无聊天记录”时显示分流。

> 历史记录归档：超出最近 10 条的更早记录已移动到 `docs/progress-archive.md`
